pyms:
  services:
    service_discovery:
      service: "tests.test_service_discovery.MockServiceDiscovery"
      autoregister: true
  config:
    DEBUG: true
    TESTING: true
    APP_NAME: "Python Microservice Service Discovery"