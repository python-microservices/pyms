language: python
python:
    - '3.6'
    - '3.7'
install:
  - pip install pipenv
  - pip install -U tox coveralls

script:
  - coverage erase
  - tox
after_success:
  - coverage combine
  - coveralls

notifications:
  email:
    - a.vara.1986@gmail.com